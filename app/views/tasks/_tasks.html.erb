<% tasks.each do |task| %>
    <%= link_to task.id, task %> : <%= task.status %> &gt; <%= task.content %><span>posted at <%= task.created_at %></span>
    <p><%= task.content %></p>
    
<% end %>
<%= paginate tasks %>